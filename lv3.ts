/**
 * 进入下一次业务迭代前，先做一些技术储备
 */


/**
 * 完善下面函数的类型，至少能表明输入和输出类型是一致的，同时保证 param 必须是 string 或 number
 */
function sameTypeInputOutput(param) {
  return param
}

const a = sameTypeInputOutput(1)

/**
 * 继续完善下面函数的类型
 */
function swap(param1, param2) {
  return [param2, param1]
}

const b = swap(1, 'a');

export default {}
